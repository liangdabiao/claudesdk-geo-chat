# GeoChat — AI GEO 内容优化分析助手 技术方案

## 一、项目概述

基于 **Claude Agent SDK** 构建的 GEO（Generative Engine Optimization）内容优化分析 Chat Web 应用。用户输入 URL，系统调用 `geo-content-optimizer` 技能，自动执行 6 阶段分析流水线：抓取标题 → 查询扩展 → 提取主查询 → 获取 AI 概览 → 搜索摘要 → 对比报告，最终生成中文内容优化建议报告。

## 二、Skill 核心分析

### 技能：geo-content-optimizer

**目录结构：**

```
.claude/skills/geo-content-optimizer/
├── SKILL.md       # 技能主定义（6阶段完整工作流）
└── README.md      # 技能说明文档
```

**特点：零依赖** — 无需 Python、无需额外 API Key，完全使用 Claude 内置工具 + MCP 服务。

### 6 阶段分析流水线

| 阶段 | 说明 | 使用工具 |
|------|------|----------|
| Phase 0 | 输出目录初始化 | Bash (mkdir) |
| Phase 1 | 抓取页面标题（H1/title） | web_reader / Playwright 回退 |
| Phase 2 | 查询扩展研究（多角度搜索） | WebSearch × 3-5 次 |
| Phase 3 | 提取主 Google 查询词 | Claude 推理 |
| Phase 4 | 获取 Google AI Overview | WebSearch + Playwright 回退 |
| Phase 5 | 搜索结果结构化摘要 | Claude 推理 |
| Phase 6 | 对比分析 + 优化建议报告 | Claude 推理 |

**工作流程：**

```
URL 输入
  → Phase 0: 创建 output/<domain-slug>/ 目录
  → Phase 1: web_reader 抓取页面 → 提取 H1/Title
  → Phase 2: WebSearch 多角度搜索扩展（3-5个子主题）
  → Phase 3: 从搜索结果提炼核心 MAIN_QUERY
  → Phase 4: 获取 Google AI Overview（Playwright 回退）
  → Phase 5: 整理搜索摘要
  → Phase 6: 对比分析 → 生成 report.md
```

### 输出文件结构

```
output/<domain-slug>/
├── report.md                 # 最终优化报告（对比表 + 行动建议）
├── query_fanout.md           # 多角度搜索原始结果
├── ai_overview.md            # Google AI 概览内容
└── query_fanout_summary.md   # 搜索结果结构化摘要
```

### 报告内容

- **摘要对比表** — 搜索扩展 vs AI 概览的多维度对比
- **共性与规律** — 搜索生态中的趋势
- **差异分析** — 各来源独特观点
- **内容缺口** — AI 概览覆盖但页面缺失的话题
- **优化建议** — 按优先级排列的具体行动措施

## 三、架构设计

```
用户浏览器 ←WebSocket→ Express Server ←→ Claude Agent SDK ←→ geo-content-optimizer
                          ↓                    ↓                    ↓
                    geo_reports/         Agent Client       WebSearch + Playwright
                    (分析报告)            (SDK封装)          + web_reader MCP
```

### 技术栈

- **Claude Agent SDK** — AI Agent 调度 + Skill 系统
- **WebSearch** — Google 搜索查询扩展
- **Playwright MCP** — 动态网页渲染回退 + Google AI Overview 抓取
- **web_reader MCP** — 网页内容抓取
- **Express + WebSocket** — HTTP 服务 + 实时通信
- **React 18 + Tailwind CSS 4** — 前端 UI（绿色主题）
- **Vite 6** — 构建工具
- **TypeScript** — 类型安全

## 四、Agent 配置

### MCP 配置

```json
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@anthropic-ai/mcp-playwright"]
    }
  }
}
```

### allowedTools

```typescript
allowedTools: [
  "Skill",                                          // 调用 geo-content-optimizer 技能
  "Task", "TodoWrite",                              // 任务管理
  "WebSearch",                                      // Google 查询扩展
  "Bash",                                           // 创建输出目录
  "Read", "Write", "Glob", "Grep",                  // 文件操作
  "mcp__web_reader__webReader",                     // 网页内容抓取
  "mcp__playwright__browser_navigate",              // 浏览器导航
  "mcp__playwright__browser_snapshot",              // 页面快照
  "mcp__playwright__browser_take_screenshot",       // 截图
  "mcp__playwright__browser_click",                 // 页面点击
  "mcp__playwright__browser_close",                 // 关闭浏览器
]
```

### systemPrompt 核心内容

```
你是 GeoChat，一个专业的 AI GEO 内容优化分析助手。

核心技能：geo-content-optimizer
- 6阶段自动化分析流水线
- 输入 URL → 抓取标题 → 查询扩展 → AI 概览获取 → 对比报告
- 零依赖：使用 WebSearch + Playwright + web_reader 完成所有操作
- 所有输出文件使用中文（简体）

工作流程：
1. 用户提供要分析的 URL
2. 调用 geo-content-optimizer 技能执行完整分析
3. Phase 0: 创建输出目录
4. Phase 1: web_reader 抓取页面标题
5. Phase 2: WebSearch 多角度查询扩展
6. Phase 3: 提取核心 Google 查询词
7. Phase 4: 获取 Google AI Overview（Playwright 回退）
8. Phase 5: 搜索摘要整理
9. Phase 6: 对比分析 + 优化报告

文件输出规则：
- 分析报告 → geo_reports/<domain-slug>/
  ├── report.md              # 最终优化报告
  ├── query_fanout.md         # 搜索扩展原始结果
  ├── ai_overview.md          # Google AI 概览
  └── query_fanout_summary.md # 搜索摘要

用中文回复用户。
```

## 五、前端设计

### 三栏布局（绿色主题）

| 区域 | 宽度 | 内容 |
|------|------|------|
| 左栏 | 280px | 分析报告文件树 + 预览 |
| 中栏 | 自适应 | 聊天消息流 + 输入框 |
| 右栏 | 220px | 执行日志（工具调用） |

### 配色方案（绿色主题）

- 主色：`emerald-600` / `emerald-500`
- 背景：`gray-950` / `gray-900`
- 边框：`gray-800`
- 脉冲：`emerald-400`
- 用户气泡：`emerald-600`

### 欢迎页模板

| 图标 | 标题 | 描述 | 提示词 |
|------|------|------|--------|
| 🔍 | GEO分析 | 网页内容优化分析 | "帮我分析这个页面的GEO优化：" |
| 📊 | 内容优化 | AI搜索内容覆盖分析 | "帮我优化这个页面的AI搜索表现：" |
| 🌐 | 竞品对比 | 多网站内容对比 | "帮我对比分析这些竞品页面：" |
| 📝 | SEO洞察 | 搜索引擎优化建议 | "帮我分析这个页面的搜索引擎优化：" |

## 六、项目结构

```
geo-chat/
├── .claude/
│   ├── settings.json              # Playwright MCP 配置
│   └── skills/
│       └── geo-content-optimizer/
│           ├── SKILL.md           # 技能主定义
│           └── README.md          # 技能说明
├── server/
│   ├── index.ts                   # Express + WebSocket 入口
│   ├── agent-client.ts            # Agent SDK 封装
│   ├── message-queue.ts           # 消息队列
│   └── logger.ts                  # 日志工具
├── src/
│   ├── App.tsx                    # 主界面（绿色主题）
│   ├── types.ts                   # 类型定义
│   ├── hooks/
│   │   ├── useWebSocket.ts        # WebSocket Hook
│   │   └── useFileUpload.ts       # 文件上传 Hook
│   └── main.tsx                   # React 入口
├── public/
│   └── index.html
├── geo_reports/                   # 分析报告输出目录
├── uploads/                       # 上传文件临时目录
├── package.json
├── tsconfig.json
├── tsconfig.server.json
├── vite.config.ts
├── tailwind.css
├── .env                           # 环境变量
└── README.md
```

## 七、端口与路由

| 项目 | 值 |
|------|-----|
| 开发端口 | 5173（Vite）→ 3013（API 代理） |
| 生产端口 | 3013 |
| WebSocket | /ws |
| 聊天 API | POST /api/chat |
| 文件上传 | POST /api/upload（multer） |
| 报告列表 | GET /api/reports |
| 报告文件 | /geo_reports/*（静态） |

## 八、环境变量

```env
ANTHROPIC_API_KEY=your-key
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
MODEL=deepseek-v4-flash
PORT=3013
```

## 九、实施步骤

1. **创建项目骨架** — package.json、tsconfig、vite.config、tailwind.css
2. **部署 Skills** — 复制 geo-content-optimizer 到 .claude/skills/
3. **实现后端** — server/ 目录（agent-client、express、websocket、upload）
4. **实现前端** — src/ 目录（App.tsx 绿色主题、hooks、types）
5. **安装依赖** — npm install
6. **验证运行** — npm run dev 测试
7. **编写 README** — 项目文档

## 十、与其他项目的差异

| 差异点 | 本项目 | 其他项目 |
|--------|--------|----------|
| 核心功能 | GEO 内容优化（URL→报告） | 研究分析 / 评论分析 |
| Skill 数量 | 1 个（6阶段流水线） | 1-5 个 |
| MCP 工具 | Playwright + web_reader | Playwright / Exa / 无 |
| 输入方式 | URL 输入（非文件上传为主） | 文件上传 / 文本输入 |
| Bash 用途 | 仅创建目录 | Python 脚本执行 / 无 |
| 输出格式 | 4个 MD 文件（报告+中间产物） | CSV+MD+HTML / 纯 MD |
| 前端主题 | 绿色 emerald | orange / cyan / blue 等 |
| 端口 | 3013 | 3007-3012 |
